export interface Activity {
  id: string;
  title: string;
  date: string;
  description: string;
  category: string;
  city: string;
  venue: string;
}
//could also use a type instead of interface - both would work in same way
//types vs interfaces??
//This was generated using https://transform.tools/json-to-typescript
